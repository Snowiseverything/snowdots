########################################################################
##  HyprSnow — v1.0.5                              Edited: 2026-04-29 ##
########################################################################

# ── COLOR SYSTEM ──────────────────────────────────────────────────────
source = ~/.cache/skwd-wall/hyprland-colors.conf

# Use explicit hex or rgba for fallbacks to ensure the gradient parser survives
$color1   ?= rgba(8839efff)
$color4   ?= rgba(cba6f7ff)
$inactive ?= rgba(000000aa)
# ──────────────────────────────────────────────────────────────────────

$mainMod = SUPER

# ── EARLY ENV (must be first exec-once) ───────────────────────────────
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
env = XDG_DESKTOP_PORTAL_HYPRLAND_FORCE_SHM,1


########################################################################
##  MONITOR                                                            ##
########################################################################

monitor = , 2560x1440@180, auto, 1


########################################################################
##  ENVIRONMENT                                                        ##
########################################################################

# Desktop session
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# NVIDIA
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = NVD_BACKEND,direct
env = __GL_GSYNC_ALLOWED,1
env = __GL_VRR_ALLOWED,1
env = __GL_MaxFramesAllowed,1
env = QT_WAYLAND_FORCE_DPI,physical
env = QT_AUTO_SCREEN_SCALE_FACTOR,1

# Toolkits
env = QT_QPA_PLATFORMTHEME,qt5ct
env = GDK_BACKEND,wayland,x11
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland
env = ELECTRON_OZONE_PLATFORM_HINT,auto


########################################################################
##  AUTOSTART                                                          ##
########################################################################

# System / session
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec-once = systemctl --user restart xdg-desktop-portal-hyprland
exec-once = hyprctl setcursor Adwaita 24
exec-once = killall -9 awww-daemon || true
exec-once = rm -f $XDG_RUNTIME_DIR/swww.socket || rm -f $XDG_RUNTIME_DIR/awww.socket
exec-once = sleep 5 && awww-daemon --format xrgb
exec-once = skwd
exec-once = sleep 7 && ~/.local/bin/wallpaper-init.sh
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = hypridle
exec-once = clipse -listen
exec-once = swaync

# Shell / UI (slight delay so Wayland is ready)
exec-once = sleep 0.5 && caelestia shell -d
exec-once = sleep 2  && hyprctl dispatch workspace 1

# RGB
exec-once = openrgb --server
exec-once = sleep 12 && openrgb --profile Blue

# Hardware
exec-once = sleep 5 && coolercontrol

# Apps (sent to their workspaces via windowrules)
exec-once = brave
exec-once = steam
exec-once = sleep 3 && trayscale --hide-window
exec-once = sleep 4 && discord


########################################################################
##  INPUT                                                              ##
########################################################################

input {
    kb_layout         = us, iq(ku_ara), ara
    kb_options        = grp:alt_shift_toggle
    numlock_by_default= false
    follow_mouse      = 1
    mouse_refocus     = false
    accel_profile     = flat
    force_no_accel    = true
    sensitivity       = 0
}

cursor {
    enable_hyprcursor  = true
    no_hardware_cursors= true
    min_refresh_rate   = 180
}


########################################################################
##  LOOK & FEEL                                                        ##
########################################################################

general {
    gaps_in          = 5
    gaps_out         = 10
    border_size      = 4
    col.active_border  = $color4 $color1 45deg
    col.inactive_border = $inactive
    resize_on_border = false
    allow_tearing    = true
    layout           = dwindle
}

decoration {
    rounding         = 10
    active_opacity   = 0.985
    inactive_opacity = 0.85

    blur {
        enabled = true
        size    = 3
        passes  = 2
    }

    shadow {
        enabled = false
    }
}

animations {
    enabled = true
    bezier  = snappy, 0.2, 0.1, 0.2, 1

    animation = windows,    1, 2.5, snappy
    animation = fade,       1, 2.5, snappy
    animation = workspaces, 1, 2,   default, slidefade 20%
    animation = border,     1, 1,   default
}

dwindle {
    pseudotile     = true
    preserve_split = true
    smart_split    = false
    force_split    = 0
}

render {
    direct_scanout = true
}

misc {
    vfr                      = false
    vrr                      = 1
    disable_hyprland_logo    = true
    disable_splash_rendering = true
    force_default_wallpaper  = 0
    enable_swallow           = true
    swallow_regex            = ^(kitty)$
    swallow_exception_regex  = ^(wev)$
    focus_on_activate        = true
    allow_session_lock_restore = true
    always_follow_on_dnd     = true
    layers_hog_keyboard_focus= true
}


########################################################################
##  WINDOW RULES                                                       ##
########################################################################

# Workspace assignments
windowrule = workspace 2 silent, match:class ^(discord|io.github.forkgram.tdesktop)$
windowrule = workspace 3 silent, match:class ^(steam)$

# Float + center
windowrule = float on,  match:class ^(clipse|skwd|dev.deedles.Trayscale|io.github.forkgram.tdesktop|swappy|mpv|coolercontrol)$
windowrule = center on, match:class ^(clipse|skwd|dev.deedles.Trayscale|io.github.forkgram.tdesktop|swappy|mpv|coolercontrol)$
windowrule = float on,  match:class ^(gamescope|re9.exe)$
windowrule = center on, match:class ^(gamescope|re9.exe)$

# Sizes
windowrule = size 1200 800, match:class ^(coolercontrol|swappy)$
windowrule = size 1000 600, match:class ^(mpv|MPV|Mpv)$
windowrule = size 622  652, match:class ^(clipse)$

# mpv extras
windowrule = workspace unset, match:class ^(mpv|MPV|Mpv)$
windowrule = rounding 10,     match:class ^(mpv|MPV|Mpv)$

# Opacity
windowrule = match:class ^(kitty)$, opacity 0.85

# Focus / misc
#windowrule = no_initial_focus 1, match:class ^(dev.deedles.Trayscale)$
windowrule = immediate on,        match:class ^(cs2)$
windowrule = stay_focused on,     match:class ^(cs2)$


########################################################################
##  KEYBINDS                                                           ##
########################################################################

# ── Wallpaper ─────────────────────────────────────────────────────────

# Normal Toggle
bind = $mainMod, W, exec, skwd wall toggle && sleep 1 && $HOME/.local/bin/wall-sync.sh

# Force a full re-application
bind = $mainMod SHIFT, W, exec, $HOME/.local/bin/skwd-safe.sh wall apply "$(jq -r '.path' ~/.cache/skwd-wall/wallpaper/current.json)"

# ── System ────────────────────────────────────────────────────────────
bind = $mainMod SHIFT, M,      exit,
bind = $mainMod SHIFT, R,      exec, killall caelestia; caelestia shell -d
bind = $mainMod SHIFT, B,      exec, hyprctl keyword decoration:blur:enabled toggle
bind = $mainMod,       C,      exec, $HOME/.local/bin/safe-kill.sh
bind = $mainMod,       O,      layoutmsg, togglesplit
bind = $mainMod,       A,      alterzorder, top
bind = $mainMod,       N,      exec, swaync-client -t -sw
bind = SHIFT,          Escape, exec, hyprctl dispatch killactive "" && pkill -9 gamescope && pkill -9 re9 && pkill -9 mangohud

# ── Power & lock ──────────────────────────────────────────────────────
bind = $mainMod,  L,	     exec, loginctl lock-session 
bind = $mainMod,  Return,    exec, ~/.local/bin/fuzzel-control.sh
bind = $mainMod,  SPACE,     exec, ~/.local/bin/app-launcher.sh
bind = CTRL ALT,  Delete,    exec, zenity --question --text="Are you sure you want to reboot?" && systemctl reboot

# ── Apps ──────────────────────────────────────────────────────────────
bind = $mainMod, B,      exec, brave --ozone-platform-hint=auto --enable-features=WaylandWindowDecorations
bind = $mainMod, D,      exec, discord
bind = $mainMod, F,      exec, thunar
bind = $mainMod, Q,      exec, kitty
bind = $mainMod, S,      exec, spotify
bind = $mainMod, T,      exec, trayscale
bind = $mainMod, V,      exec, kitty --class clipse -e clipse
bind = $mainMod, X,      fullscreen, 0
bind = $mainMod, Z,      exec, $HOME/.local/bin/hypr-float.sh
bind = CTRL SHIFT, ESCAPE, exec, kitty btop

# ── Hardware & media ──────────────────────────────────────────────────
binde = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
binde = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioNext,        exec, playerctl next
bindl = , XF86AudioPause,       exec, playerctl play-pause
bindl = , XF86AudioPlay,        exec, playerctl play-pause
bindl = , XF86AudioPrev,        exec, playerctl previous

# Monitor brightness (DDC)
binde = $mainMod, Prior, exec, ddcutil setvcp 10 + 10
binde = $mainMod, Next,  exec, ddcutil setvcp 10 - 10

# Screenshots
bind = $mainMod SHIFT, S,      exec, ~/.local/bin/shot-smart.sh region
bind = $mainMod ALT,   S,      exec, ~/.local/bin/shot-smart.sh window
bind = $mainMod,       Insert, exec, ~/.local/bin/shot-smart.sh full

# Misc
bind = $mainMod SHIFT, F, exec, /home/snow/.local/bin/sun-schedule.sh toggle

# ── Focus ─────────────────────────────────────────────────────────────
bind = ALT, left,  movefocus, l
bind = ALT, right, movefocus, r
bind = ALT, up,    movefocus, u
bind = ALT, down,  movefocus, d

# ── Resize ────────────────────────────────────────────────────────────
bind = ALT CTRL, left,  resizeactive, -50 0
bind = ALT CTRL, right, resizeactive,  50 0
bind = ALT CTRL, up,    resizeactive,  0 -50
bind = ALT CTRL, down,  resizeactive,  0  50

# ── Move window ───────────────────────────────────────────────────────
bind = ALT SHIFT,      left,  movewindow, l
bind = ALT SHIFT,      right, movewindow, r
bind = ALT SHIFT,      up,    movewindow, u
bind = ALT SHIFT,      down,  movewindow, d
bind = $mainMod SHIFT, H,     movewindow, l
bind = $mainMod SHIFT, L,     movewindow, r
bind = $mainMod SHIFT, K,     movewindow, u
bind = $mainMod SHIFT, J,     movewindow, d

# ── Swap window ───────────────────────────────────────────────────────
bind = $mainMod ALT, left,  swapwindow, l
bind = $mainMod ALT, right, swapwindow, r
bind = $mainMod ALT, up,    swapwindow, u
bind = $mainMod ALT, down,  swapwindow, d

# ── Workspaces ────────────────────────────────────────────────────────
bind = $mainMod, left,  workspace, r-1
bind = $mainMod, right, workspace, r+1
bind = $mainMod, TAB,   workspace, previous
bind = ALT,      TAB,   cyclenext
bind = $mainMod, mouse_up,   workspace, e+1
bind = $mainMod, mouse_down, workspace, e-1

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move to workspace (follow window)
bind = $mainMod SHIFT, 1,     movetoworkspace, 1
bind = $mainMod SHIFT, 2,     movetoworkspace, 2
bind = $mainMod SHIFT, 3,     movetoworkspace, 3
bind = $mainMod SHIFT, 4,     movetoworkspace, 4
bind = $mainMod SHIFT, 5,     movetoworkspace, 5
bind = $mainMod SHIFT, 6,     movetoworkspace, 6
bind = $mainMod SHIFT, 7,     movetoworkspace, 7
bind = $mainMod SHIFT, 8,     movetoworkspace, 8
bind = $mainMod SHIFT, 9,     movetoworkspace, 9
bind = $mainMod SHIFT, 0,     movetoworkspace, 10
bind = $mainMod SHIFT, left,  movetoworkspace, r-1
bind = $mainMod SHIFT, right, movetoworkspace, r+1

# Move to workspace (silent)
bind = $mainMod CTRL, 1,     movetoworkspacesilent, 1
bind = $mainMod CTRL, 2,     movetoworkspacesilent, 2
bind = $mainMod CTRL, 3,     movetoworkspacesilent, 3
bind = $mainMod CTRL, 4,     movetoworkspacesilent, 4
bind = $mainMod CTRL, 5,     movetoworkspacesilent, 5
bind = $mainMod CTRL, 6,     movetoworkspacesilent, 6
bind = $mainMod CTRL, 7,     movetoworkspacesilent, 7
bind = $mainMod CTRL, 8,     movetoworkspacesilent, 8
bind = $mainMod CTRL, 9,     movetoworkspacesilent, 9
bind = $mainMod CTRL, 0,     movetoworkspacesilent, 10
bind = $mainMod CTRL, left,  movetoworkspacesilent, r-1
bind = $mainMod CTRL, right, movetoworkspacesilent, r+1

# ── Mouse ─────────────────────────────────────────────────────────────
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
